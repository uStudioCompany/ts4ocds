{
  "name": "ts4ocds",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "ts4ocds:build": "yarn --cwd ./packages/ts4ocds build",
    "website:start": "yarn ts4ocds:build && yarn --cwd ./packages/docs-website start",
    "website:build": "yarn ts4ocds:build && yarn --cwd ./packages/docs-website build"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
