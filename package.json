{
  "name": "ts4ocds",
  "version": "0.0.0.",
  "repository": "https://github.com/uStudioCompany/ts4ocds.git",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "ts4ocds:build": "yarn --cwd ./packages/ts4ocds build",
    "ts4ocds:deploy": "yarn --cwd ./packages/ts4ocds deploy",
    "website:start": "yarn --cwd ./packages/docs-website start",
    "website:build": "yarn --cwd ./packages/docs-website build"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
